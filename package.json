{
  "name": "jasmine-node-gulp-coffee",
  "version": "1.0.0",
  "scripts": {
    "jasmine": "jasmine-node out/spec",
    "gulp": "gulp --require coffee-script/register",
    "clean": "gulp clean",
    "build": "gulp clean && gulp",
    "watch": "npm run build && gulp watch",
    "test": "npm run build && npm run jasmine"

  },
  "devDependencies": {
    "coffee-script": "^1.10.0",
    "gulp": "^3.9.0",
    "gulp-coffee": "^2.3.1",
    "gulp-rimraf": "^0.2.0",
    "jasmine-node": "^1.14.5"
  }
}
